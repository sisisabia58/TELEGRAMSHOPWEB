<%
    const stokBase = `/produk/${produk.id}/varian/${varian.id}/stok`;

%>
<!DOCTYPE html>
<html lang="id">
<head>
    <%- include('partials/head') %>
</head>
<body>
    <a href="#main-content" class="skip-link">Skip to main content</a>
    
    <%- include('partials/sidebar') %>
    
    <main class="main-content" id="main-content">
        <%- include('partials/topbar') %>
        
        <div class="container">
            <div class="card" style="max-width: 900px; margin: 0 auto;">
                <div class="card-header">
                    <h2 class="card-title">➕ Tambah Stok Baru</h2>
                </div>
                <div class="card-body">
                    <!-- Product Info -->
                    <div style="background: #f8f9fa; padding: 20px; border-radius: 10px; margin-bottom: 30px; border-left: 4px solid var(--color-accent);">
                        <p style="margin: 0 0 8px 0;"><strong>Produk:</strong> <%= produk.nama %></p>
                        <p style="margin: 0;"><strong>Varian:</strong> <%= varian.label %> · <code><%= varian.kode %></code></p>
                    </div>

                    <% if (error) { %>
                        <div class="alert alert-error">
                            ❌ <%= error %>
                        </div>
                    <% } %>

                    <form method="POST" action="<%= stokBase %>/tambah">
                        <div class="form-group">
                            <label class="form-label">
                                Data Stok (Satu Item Per Baris) <span class="required">*</span>
                            </label>
                            <textarea 
                                id="data_stok" 
                                name="data_stok" 
                                class="form-textarea"
                                required
                                placeholder="email1:password1&#10;email2:password2&#10;email3:password3"
                                autofocus
                                style="min-height: 250px; font-family: 'Courier New', monospace; font-size: 13px;"
                            ></textarea>
                            <div class="form-help">
                                💡 Masukkan data stok, satu item per baris. Contoh: email:password atau data lainnya sesuai format produk.
                            </div>
                        </div>

                        <div class="form-actions" style="display: flex; gap: 12px; margin-top: 30px;">
                            <button type="submit" class="btn btn-success" style="flex: 1;">
                                💾 Tambah Stok
                            </button>
                            <a href="<%= stokBase %>" class="btn" style="background: #6c757d; flex: 1; text-align: center;">
                                ❌ Batal
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </main>
    <%- include('partials/mobile-tabs') %>
    
    <!-- Quick Actions -->
    <div class="quick-actions">
        <button class="quick-action-btn" onclick="window.scrollTo({top: 0, behavior: 'smooth'})" aria-label="Scroll to top" title="Scroll to top">
            ↑
        </button>
    </div>
    
    <script>
        // Sidebar Toggle
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('collapsed');
            sidebar.classList.toggle('open');
        }
        
        // Close sidebar on mobile when clicking outside
        document.addEventListener('click', (e) => {
            const sidebar = document.getElementById('sidebar');
            const toggleBtn = document.querySelector('.sidebar-toggle');
            
            if (window.innerWidth <= 768 && 
                sidebar.classList.contains('open') && 
                !sidebar.contains(e.target) && 
                !toggleBtn.contains(e.target)) {
                sidebar.classList.remove('open');
            }
        });
        
        // Handle responsive sidebar
        window.addEventListener('resize', () => {
            const sidebar = document.getElementById('sidebar');
            if (window.innerWidth > 768) {
                sidebar.classList.remove('open');
            }
        });
    </script>
    
    <!-- Real-time Notifications -->
</body>
</html>
